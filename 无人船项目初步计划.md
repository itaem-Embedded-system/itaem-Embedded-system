# 关于无人船项目的初步调研

## 1. 项目现状

当前接手的工程是一个基于 STM32F103C8 的小船主控项目，开发环境为 Keil，底层框架使用 STM32F1 标准外设库。

当前项目本质上是一个开环控制遥控船：通过 2.4G 遥控链路接收控制数据，输出两路 PWM 驱动左右推进，并在失联时执行停机保护。当前工程本身并没有定位、导航、采样、视觉、路径规划、水质检测等无人化能力基础。


## 3. 当前技术重点

如果后续还需要回头理解现有代码，主要看 2.4G 接收链路、PWM 输出和失联停机这三部分即可，相关代码集中在 [User/main.c](User/main.c)、[Hardware/rf2G4.c](Hardware/rf2G4.c)、[Hardware/PWM.c](Hardware/PWM.c) 和 [Hardware/timer.c](Hardware/timer.c)。但如果后续主线转向飞控平台，这部分只需要作为背景参考，不必投入过多篇幅。

## 4. 路线选择整理

结合目前讨论，以及老师提出的八月份前推进到定点巡航到点的目标，后续主要有两条路线值得考虑：

1. 自研 STM 平台路线。
2. 飞控平台路线。

### 4.1 F1、F4、H7 的讨论


#### F4

F4 是较典型的自研平台选择，资料多、生态成熟，适合做后续自主平台预研；但在当前以定点巡航到点为近期目标的前提下，不宜作为主推进路线。

#### H7


H7 更适合作为长期平台储备，资源余量更大，但开发门槛也更高，同样不适合作为当前阶段的最优推进方案。

### 4.2 自研 STM F4/H7 路线

这条路线与团队现有 STM 积累衔接更自然，也更利于沉淀自主设计能力；但在当前工期下，参数系统、日志系统、模式管理和稳定联调能力都需要自行补齐，风险较高。因此更适合作为后续长期自研平台或并行预研方向，而不建议作为当前阶段的主推进路线。

### 4.3 成品飞控平台路线

飞控平台并不只用于无人机。可以将其理解为一种面向无人系统的成熟控制核心板：硬件上通常集成 MCU、电源管理、IMU、PWM 输出和多种传感器接口，软件上通常配套参数系统、日志系统、模式管理、失联保护和地面站支持。

结合目前能直接用于船体任务、文档完善度和社区成熟度来看，当前更推荐采用 ArduPilot Rover/Boat 路线。其官方文档已经明确支持 Boat，并支持 Auto、Guided、RTL、SmartRTL、Mission Planning、Failsafe、Mission Planner 等完整链路，整体成熟度明显高于继续从底层自研推进。

其优势在于：

1. 能明显减少重复造轮子。
2. 更容易快速进入系统集成和任务验证阶段。
3. 已具备较成熟的任务模式、失联保护、参数系统和日志系统。
4. 更适合后续扩展 GNSS、罗盘、遥测、电池监测和伴随计算机。
5. 更符合当前阶段“先完成定点巡航目标，再逐步扩展能力”的推进节奏。

其不足在于：

1. 团队当前缺乏使用经验。
2. 学习重点会从底层实现转向参数配置、调参流程和系统理解。
3. 是否允许使用仍需结合比赛规则确认。
4. 短期内对飞控硬件和生态存在一定依赖。

### 4.4 当前建议

基于老师要求的八月份推进到定点巡航到点的目标，预估可用于开发、联调、下水测试和问题回改的总时间约为 350 到 400 小时。在这一时间约束下，自研 F4 路线需要自行补齐硬件、底层驱动、传感器接入、控制链路和参数系统，容错空间较小，容易把时间消耗在底层问题和反复下水调试上。

因此，当前更现实的建议是立即启动飞控平台选型，优先考虑成熟度更高的 ArduPilot Rover/Boat 方案，配套 Mission Planner 或 QGroundControl 完成基础配置、航点规划、参数调试和任务验证。若硬件选型、传感器接入和调参推进顺利，采用成品飞控路线，有希望在 7 月前后进入定点巡航的稳定验证阶段。

### 4.5 飞控学习路线

若采用飞控平台路线，建议按“先跑通，再理解，再优化”的顺序推进，避免一开始就陷入底层源码和复杂算法。

第一步，先建立整体认识。主要搞清楚飞控、地面站、遥控器、GNSS、罗盘、电调、电机、遥测链路之间的关系，理解飞控系统的基本工作流。

第二步，完成硬件选型与连线。确定飞控板、电源模块、GNSS+罗盘、遥测模块、接收机、电调和推进电机，先完成上电、姿态识别、遥控输入、PWM 输出和地面站通信。

第三步，完成官方首次配置流程。按照 ArduPilot Rover/Boat 官方流程完成固件烧录、传感器校准、遥控校准、模式设置、失联保护和基础参数检查，确保系统具备安全下水条件。

第四步，先实现手动遥控与基础安全。优先验证手动模式、失联保护、返航触发、电池监测和日志记录，先把“能安全下水、能安全回来”做好。

第五步，补齐定位与航向相关能力。完成 GNSS、罗盘、航向与推进方向的联调，确认坐标、航向、转向响应和实际水面表现一致，这是后续定点巡航的前提。

第六步，进入任务模式与定点巡航。学习使用 Mission Planner 或 QGroundControl 进行航点编辑、任务上传、自动模式切换、日志回放和参数调整，逐步实现定点到达、路径跟踪和位置保持。

第七步，再做二次开发和上层扩展。在主链路稳定后，再考虑树莓派、视觉、水质采样、任务联动和自定义通信协议，而不是一开始就同时铺开。

## 5. 当前项目主要推进方向

对于老师提出的地图标点后无人船自动巡航到点、水质采样，再到联合无人机的长期目标，建议项目采用“飞控平台优先落地 + 伴随计算机后续扩展”的双层思路。当前阶段先利用成熟飞控平台完成定点巡航到点这一近期目标，后续再扩展 AI、高级导航和多平台协同能力。

树莓派、AI、无人机协同等内容暂不作为当前主线，可作为后续独立方向预研，请机器人方向同学知悉。

### 第一阶段：先把飞控主链路跑通

这是当前最重要的阶段，核心是先把飞控、地面站、传感器和推进执行链路完整打通。
重点任务如下：

1. 飞控选型。
2. 地面站安装与连接。
3. 传感器接入与校准。
4. 遥控与推进输出打通。
5. 失联保护与基础安全验证。

### 第二阶段：完成定点巡航基础能力

这一阶段的目标是让系统具备定点到达和基础自动运行能力。

重点任务如下：

1. GNSS 定位稳定。
2. 航向数据稳定。
3. 任务模式跑通。
4. 简单航点跟踪。
5. 基础到点验证。

### 第三阶段：整理工程化与水面联调

这一阶段主要是把系统提升到“可维护、可复现、可稳定下水”。

重点任务如下：

1. 参数整理。
2. 校准流程固化。
3. 日志回放与问题定位。
4. 模式切换与故障处理。
5. 水面测试流程整理。

### 第四阶段：做后续扩展

这一阶段的目标是在主链路稳定后，再逐步增加更高层任务能力。

重点任务如下：

1. 树莓派或伴随计算机接入。
2. 水质采样任务联动。
3. 自定义通信与数据回传。
4. 视觉、AI 或联合无人机方向预研。

（如果后续团队仍希望建设完全自主的平台，再考虑启动 STM F4/H7 自研路线会更合理，此时已有更明确的需求边界和测试经验作为支撑。）

## 6. 总结

总体来看，当前项目已经完成遥控船最基础的控制，但仍停留在遥控船原型阶段。结合当前八月份前完成定点巡航到点的目标，应该完全舍弃。新工程开展工作，优先采用成熟飞控平台路线，先完成系统集成、参数配置、传感器联调和任务验证；自研 STM F4/H7 路线则作为后续长期预研方向保留。
