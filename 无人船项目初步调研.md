# 关于无人船项目的初步调研

## 1. 项目现状

当前接手的工程是一个基于 STM32F103C8 的小船主控项目，开发环境为 Keil，底层框架使用 STM32F1 标准外设库。

当前项目本质上是一个具备基础控制闭环的遥控船：能够通过 2.4G 无线模块接收遥控指令，解析控制数据并输出两路 PWM，驱动左右推进实现基础前进和转向，并在失联时执行停机保护。相关参考代码主要在 [User/main.c](User/main.c)、[Hardware/rf2G4.c](Hardware/rf2G4.c)、[Hardware/PWM.c](Hardware/PWM.c) 和 [Hardware/timer.c](Hardware/timer.c)。需要特别说明的是，目前继承到的只有小船主控代码，没有遥控器端代码，也没有定位、导航、采样、视觉、路径规划、水质检测等无人化能力基础。

## 2. 当前已经做到的内容

当前工程已经实现了遥控船的最小可运行，包括：2.4G 模块初始化和连通性检测、固定长度控制包接收、控制数据到左右两路 PWM 的映射、基础前进与转向动作，以及失联后的推进归零。
与此同时，工程中已经具备 SPI、PWM、定时器、LED、OLED、按键、Delay 等基础驱动模块，可作为后续 HAL 迁移时的参考基础；
其中失联停机逻辑已经形成“收包重置计时、超时判定无效、无效后停机”的基本安全闭环，后续应优先保留和完善。

## 3. 当前技术重点

当前项目的技术重点可以概括为三项：
第一，2.4G 无线接收链路，这是当前系统的输入入口，也是 HAL 重构和平台迁移时的第一优先级模块，主要参考 [User/main.c](User/main.c) 和 [Hardware/rf2G4.c](Hardware/rf2G4.c)；
第二，PWM 产生与推进输出，这是后续遥控模式和自主模式共用的执行层基础，主要参考 [Hardware/PWM.c](Hardware/PWM.c)；
第三，失联停机逻辑，这是当前系统最基本的安全闭环，超时判定在 [Hardware/timer.c](Hardware/timer.c)，执行停机在 [User/main.c](User/main.c)。

## 4. 当前项目定位

从代码和硬件能力来看，当前项目更准确的定位是遥控船。
它已经可以完成基础遥控执行，但还不具备完整无人船所需的定位导航、自动返航、航点跟踪、水面采样任务管理、路径规划和水藻监测等上层能力，因此原计划书中的相关目标目前还没有硬件和软件基础支撑。

## 5. 路线选择整理

结合目前讨论，后续主要有两条路线值得考虑：

1. 自研 STM 平台路线。
2. 飞控平台路线。

### 5.1 F1、F4、H7 的讨论

#### F1

F1 适合作为当前继承工程的分析和过渡平台，也能支撑第一阶段，甚至部分第二阶段工作。但如果目标是长期推进到第三、第四阶段，F1 资源会越来越紧，后续扩展成本较高。

因此，F1 更适合作为现状基础，不适合作为长期正式平台。（关于各阶段的工作详见第六章）

#### F4

F4 是较稳妥的自研平台选择。它相对容易上手，资料多，能够较平衡地支持：

1. HAL 新工程。
2. 传感器接入。
3. 低阶自动控制。
4. 基础工程化能力。

如果团队坚持自研，同时希望兼顾开发难度与性能余量，F4 是现实且合理的选择。

#### H7

H7 更适合作为长期平台，资源余量更大，后续扩展 GNSS、IMU、日志、通信和状态管理会更从容。但其开发门槛和底层复杂度也更高。

如果团队目标明确，准备长期投入，并希望尽量避免二次迁平台，那么 H7 的长期价值高于 F4。

### 5.2 自研 STM F4/H7 路线

这条路线更符合团队现有能力，因为当前积累主要集中在 STM 外设、底层驱动和小型控制系统开发。

其优势在于：

1. 与当前技术栈衔接自然。
2. 适合体现自主设计和实现能力。
3. 架构、外设和控制逻辑都可以按项目需要完全定制。

其不足在于：

1. 参数系统、日志系统、模式管理、故障处理等都需要自行补齐。
2. 工程复杂度会随着项目推进快速上升。
3. 对团队的软件架构能力要求较高。

### 5.3 成品飞控平台路线

飞控平台并不只用于无人机。以 ArduPilot Rover/Boat 为例，官方已经支持地面车辆、船和帆船，并具备：

1. 模式管理。
2. 失联保护。
3. 参数系统。
4. 日志系统。
5. 任务模式。
6. 地面站支持。
7. 与伴随计算机协同。

其优势在于：

1. 能明显减少重复造轮子。
2. 更容易快速进入系统集成和任务验证阶段。
3. 更适合后续扩展复杂通信和伴随计算机方案。
4.在本学期内能够完成定点巡航目标

其不足在于：

1. 团队当前缺乏使用经验。
2. 学习重点会从底层实现转向系统配置和框架理解。
3. 是否允许使用往往受比赛规则限制。
4. 价格高昂，且不完全开源（如 Pixhawk 4 Mini）。
5. 可能会被限制在特定硬件平台上，后续扩展受限。

### 5.4 当前建议

基于当前阶段，建议如下：

1. 近期以用hal库复刻当前功能为主
2. 平台选型不建议继续围绕 F1 长期投入。
3. 若追求稳妥和团队可控，优先考虑 F4。
4. 若目标更长远且希望降低未来再次迁平台的概率，可直接考虑 H7。
5. 即使暂时不采用飞控平台，也应主动借鉴其参数系统、模式管理、failsafe、日志和地面站思路。

## 6. 当前项目主要推进方向

对于老师提出的地图标点后无人船自动巡航到点，水质采样，再到联合无人机的长期目标，推荐项目采用树莓派加上MCU 的双层架构。复杂功能诸如 AI 识别算法，高级地图导航，应该部署在高算力树莓派上。本调研以“基于高实时性 MCU 的基础无人船方案”为主线推进，主要由嵌入式方向同学跟进。树莓派、AI、无人机协同等内容暂不作为当前主线，可作为后续独立方向预研，请机器人方向同学知悉。

### 第一阶段：把现有底盘层做完整

这是当前最重要的阶段，目标是把现有遥控船底层能力迁移到新的 HAL 工程。

重点任务如下：

1. HAL 重构。
2. RF 接收稳定，并预留或增加通信方式。
3. PWM 输出稳定。
4. 失联停机完善。
5. 参数化。

相关源码参考位置如下：

1. 通信主流程可先看 [User/main.c](User/main.c) 中的 RF2G4_Init、RF2G4_Check、RF2G4_RX_Mode 和 RF2G4_Rx_Packet 调用位置；具体 2.4G 驱动实现主要在 [Hardware/rf2G4.c](Hardware/rf2G4.c)。
2. PWM 输出可重点看 [Hardware/PWM.c](Hardware/PWM.c) 中的 PWM_Init、PWM_SetCompare1、PWM_SetCompare2、Motor1_speedset 和 Motor2_speedset。
3. 停机功能分两处看：超时判定在 [Hardware/timer.c](Hardware/timer.c) 的 TIM3_IRQHandler 中，真正执行停机在 [User/main.c](User/main.c) 中对 F_2G4_Command_Valid 的判断分支。

### 第二阶段：补基础感知

这一阶段的目标是让系统开始知道自己的状态。

重点任务如下：

1. 电压检测。
2. IMU。
3. 磁力计。
4. GNSS，即包括北斗在内的全球定位系统。

### 第三阶段：做低阶自动控制

这一阶段的目标是从遥控执行层过渡到基础自动控制层。

重点任务如下：

1. 航向保持。
2. 简单定速。
3. 简单返航。
4. 简单航点跟踪。

### 第四阶段：整理工程化

这一阶段主要是把系统提升到“可维护、可扩展”。

重点任务如下：

1. 参数存储。
2. 校准流程。
3. 状态机。
4. 故障处理。
5. 简单日志。

## 7. 总结

总体来看，当前项目已经完成遥控船最基础的控制，但仍停留在遥控船原型阶段，不具备真正无人船所需的感知、控制和工程化能力。后续建议继续按照“四阶段主线”推进，即先完成底盘层和 HAL 重构，再补基础感知、低阶自动控制和工程化整理；在路线选择上，以自研 STM F4/H7 为主线，同时借鉴成熟飞控体系的方法论，为后续更高阶段的平台演进保留空间。
