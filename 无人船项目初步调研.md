# 关于无人船项目的初步调研

## 1. 项目背景

当前接手的工程是一个基于 STM32F103C8 的无人船主控项目，开发环境为 Keil，底层框架使用 STM32F1 标准外设库。结合现有代码结构和功能实现情况来看，这个项目是一个已经打通核心控制链路的原型工程。

从现有实现判断，这套系统的主要目标是完成遥控模式下的船体基础运动控制，即通过 2.4G 无线链路接收控制指令，再将遥控输入转换为双路 PWM 输出，驱动左右推进装置，实现前进和转向，并在失联时进行停机保护。注意：继承的并没有关于遥控器部分的代码，只有小船主控

## 2. 当前已经做到的内容

### 2.1 已经打通基础控制闭环

当前工程最重要的成果，是已经实现了无人船控制
1. 主控芯片能够正常启动并完成外设初始化。
2. 2.4G 无线模块能够完成初始化和连通性检测。
3. 主控能够持续接收遥控端发送的 14 字节控制数据包。
4. 程序已经能够解析关键摇杆数据，并根据输入生成两路 PWM。
5. 两路 PWM 已经用于控制左右推进输出，形成前进和转向动作。
6. 当一段时间没有接收到新的控制指令时，系统能够触发停机保护。



### 2.2 已经具备基础驱动能力

现有工程中，已经完成了多类底层驱动模块的封装，具备继续扩展的基础：

1. SPI 驱动：用于 2.4G 模块通信。
2. PWM 驱动：用于输出左右推进控制信号。
3. 定时器驱动：用于超时计时和失联保护。
4. LED 驱动：用于状态指示。
5. OLED 驱动：用于基础显示和调试。
6. 按键驱动：用于本地输入扩展。
7. Delay 驱动：用于延时和基础时序控制。

在移植于hal库时需要注意能不能直接用

### 2.3 已经具备基础安全保护意识

项目中已经加入了较为关键的失联保护逻辑。

具体表现为：

1. 主循环每当收到一次新的遥控指令，就会重置控制有效计时。
2. 定时器中断持续统计未收到新指令的时间。
3. 当超过约 0.3 秒没有接收到新命令时，程序会将命令标志置为无效。
4. 在命令失效状态下，推进输出会被强制归零。

对于无人船这类运动平台来说，这虽然还只是基础级保护，但它非常重要，已经考虑到最基本的运行安全问题。

## 3. 当前技术重点在哪里

结合现有工程内容，当前项目最核心的技术重点主要集中在以下三个方面

### 3.1 2.4G 无线接收链路

这是当前工程的第一核心重点。

项目已经实现基于 SI24R1 兼容芯片的无线收发驱动，能够完成以下工作：

1. 寄存器读写。
2. 地址配置。
3. 模块连接检测。
4. 接收模式配置。
5. 固定长度控制包接收。

从当前业务逻辑来看，船体控制的起点不是传感器、不是自主导航，而是遥控数据包。因此，2.4G 接收链路是整个系统中最先必须稳定的部分。

如果后续做 HAL 重构或换到 F4，这一部分仍然是迁移优先级最高的模块之一。

### 3.2 PWM 产生与推进控制输出

这是当前工程的第二核心重点。

项目当前已经通过定时器输出两路 PWM 信号，用于控制左右推进装置。写代码前需要先了解现在用的是什么推进


1. 它直接决定船能不能动。
2. 它直接决定左右推进差速控制能不能实现。
3. 它是后续遥控模式和自主模式共用的执行层基础。

从架构角度讲，PWM 输出层应该尽量保持简单稳定，因为将来无论控制来源是遥控器、自动航点还是避障策略，最终都需要落到这两路推进控制上。

### 3.3 控制有效性判断与失联停机

这是当前工程的第三核心重点。

现有逻辑虽然简单，但已经具备完整的基本思路：

1. 收到命令则认为控制链路有效。
2. 长时间收不到命令则判定为失联。
3. 失联后停止推进输出。

项目已经具备无人平台最基础的安全闭环

## 4. 当前控制逻辑的实现特点

### 4.1 现阶段偏向遥控执行层，而不是完整无人系统

从现有代码实现来看，当前项目重点仍然放在“遥控控制船体运动”这一层，并没有进入完整无人船系统应有的上层能力。

也就是说，该船现阶段更准确的定位是：遥控船

一个已经具备基础遥控执行能力的无人船原型控制板。

完全不能实现北斗定位导航，水面采样。至于发的项目计划书所说的路径规划和水藻监测，根本没有任何硬件和软件的基础和实现

### 4.2 当前控制逻辑以直接映射为主

程序当前主要采用摇杆输入到 PWM 输出的直接映射方式即最基础的直接控制。

这类实现的优点是：

1. 简单。
2. 直观。
3. 易于快速联调。
4. 适合原型验证。

但它也意味着当前控制层的成熟度还不高，后续如果要实现更平滑的操控、更合理的差速转向，或者自动控制功能，控制算法部分还需要进一步整理与抽象。

## 5. 当前工程的阶段判断

综合功能完成度和工程成熟度，我认为这个项目当前最适合的阶段判断是：

遥控船可测原型。

这个结论的依据是：

1. 它已经具备真实的控制闭环。
2. 它已经可以围绕主控、无线和推进链路开展上电联调与水面测试。
3. 它已经具备基础失联保护。
4. 但它还没有形成完善的控制架构、参数管理和无人化任务层。
5. 它还不适合被视为产品版本。
。

## 6. 当前项目中值得重视的补充方面

除了已经明确提出的 2.4G 接收和 PWM 输出之外，我认为还有几个方面值得在后续推进中重点关注。

### 6.1 控制逻辑与底层驱动要尽快解耦

当前主控制逻辑比较集中，后续如果继续扩展功能，容易出现主循环不断膨胀的问题。

因此，后续迁移到 HAL 工程时，不能只做接口替换，还应同时把架构拆成更清晰的层次，这一点详见嵌入式软件开发规范，

这一步比单纯换库更重要，因为它决定未来是否容易迁移到 F4 或更高性能平台。

### 6.2 参数化是后续工程化的关键一步

当前很多控制阈值、PWM 中值和映射关系都直接写在代码里。这样的方式在原型阶段是可接受的，但不利于团队协作和后续调试。

后续建议尽快把以下内容参数化，宏定义化：

1. 摇杆死区。
2. PWM 中值。
3. 最大最小脉宽。
4. 超时阈值。
5. 推进左右增益。

一旦参数化完成，后面调试电机、电调和船体姿态时，效率会明显提高。

### 6.3 当前“无人船”的无人能力仍明显不足

如果从严格意义上的无人船角度看，当前工程还缺少若干关键能力，包括：

1. GPS 定位。
2. IMU 姿态感知。
3. 电源与电池电压监测。
4. 模式管理机制。
5. 自主航行逻辑。
6. 任务与状态管理——rtos系统。

因此，当前工程更适合作为无人船底层执行控制板的起点，而不是完整无人系统的终点。

## 7. 对后续推进方向的建议

### 7.1 移植入hal库

议第一阶段只聚焦以下目标：

1. 新建 HAL 工程。
2. 复现 2.4G 接收。
3. 复现两路 PWM 输出。
4. 复现失联停机逻辑。
5. 复现旧版主控的基础控制行为。

这五项完成，新的 HAL 工程就已经真正接住了旧工程最核心的成果，预测两周内可以完全复刻遥控船。

### 7.2 建议面向未来换 F4 的方向提前做结构准备

如果后续确实考虑换到 F4 或更高性能芯片，那么当前这次重构就应该提前考虑可移植性，也是参考规范写可移植性的代码。


### 7.3 团队协作时按技术点拆分推进

这个项目非常适合拆成小技术点并行推进，：

1. HAL 工程骨架。
2. RF 接收模块。
3. PWM 输出模块。
4. 失联保护模块。
5. 控制混控模块。
6. 参数整理与测试验证。



## 8. 总结

总体来看，当前无人船项目已经完成了

它已经实现了从遥控输入到推进输出的基础控制闭环，并且具备了最基本的失联停机保护能力。

现阶段最值得重视的技术重点，主要就是：

1. 2.4G 接收链路。
2. PWM 产生与执行输出。
3. 失联保护逻辑。

这三部分决定了当前工程是否真正可运行，也决定了后续 HAL 重构时最先应迁移哪些内容。

如果后续继续推进，建议不要把当前工程简单视为一个旧项目去修补，而应把它视为一个已经验证方向正确的原型基础，在此基础上完成结构整理、HAL 迁移和面向更高性能平台的演进。
